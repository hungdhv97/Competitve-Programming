#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<char> a;
    a.push_back('0');
    a.push_back('0');
    a.push_back('1');
    set<char> s(a.begin(), a.end());
    cout << a.size() << " " << s.size();
    if(1 != 1)
    {
        cout<<100;
    }
}
